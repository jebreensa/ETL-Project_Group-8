CREATE TABLE practice_nba (
	PLAYER_ID INT,
	SEASON_ID VARCHAR(10),
	LEAGUE_ID INT,
	TEAM_ID INT,
	TEAM_ABBREVIATION VARCHAR(4),
	PLAYER_AGE DECIMAL,
	GP INT,
	GS INT,
	MIN DECIMAL,
	FGM INT,
	FGA INT,
	FG_PCT DECIMAL,
	FG3M DECIMAL,
	FG3A DECIMAL,
	FG3_PCT DECIMAL,
	FTM INT,
	FTA INT,
	FT_PCT DECIMAL,
	OREB DECIMAL,
	DREB DECIMAL,
	REB DECIMAL,
	AST INT,
	STL DECIMAL,
	BLK DECIMAL,
	TOV DECIMAL,
	PF INT,
	PTS INT,
	full_name VARCHAR(30),
	first_name VARCHAR(30),
	last_name VARCHAR(30),
	is_active VARCHAR(10));
	
	SELECT * FROM practice_nba LIMIT 10
	
	DROP TABLE practice_nba